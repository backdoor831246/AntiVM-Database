# AntiVM-Database
Header-only C++ module containing curated blacklists for system environment validation, including HWIDs, MAC addresses, processes, hostnames, usernames, BIOS versions, disk serials, GPUs, IPs, and profile GUIDs. Designed for integrity checks, anti-debugging, and environment risk detection.
